#include <stdio.h>
#include <malloc.h>

#define ElemType int;
typedef struct DNode {                              //定义双链表结点类型
    ElemType data;                                  //数据域
    struct DNode *prior, *next;                     //前驱和后继指针
};

void main(int argc, char const *argv[])
{
    
    return;
}
