/* 

1.  初始化线性表L  InitList(&L)      
2.  销毁线性表L  DestoryList(&L)    
3.  清空线性表L  ClearList(&L)   
4.  求线性表L的长度  ListLength(L)
5.  判断线性表L是否为空  ListEmpty(L) 
6.  获取线性表L中的某个数据元素内容  GetElem(L,i,&e) 
7.  检索值为e的数据元素  LocateELem(L,e) 
8.  返回线性表L中cur_e的直接前驱元素  PriorElem(L,cur_e,&pre_e) 
9.  返回线性表L中cur_e的直接后继元素  NextElem(L,cur_e,&next_e)       
10. 在线性表L中插入一个数据元素  ListInsert(&L,i,e) 
                                                        (1≦i≦ ListLength(L)+1)
11. 删除线性表L中第i个数据元素  ListDelete(&L,i,&e)
                                                      (1≦i≦ ListLength(L))
12. 输出线性表L中的每个数据元素 ListTraverse(L)
*/


#include <stdio.h>
#include <stdlib.h>


//合并两个线性表 
void union()
{
} 

void main()
{
	
}



 
